## @editing: Sora [br]
## @describe: 实体行为基类 - 定义实体可执行行为的抽象接口
## 
## 该抽象类为所有实体行为提供统一的框架。行为需要与行为组件绑定使用，
## 通过实现类来定义具体的行为逻辑，如死亡掉落、技能释放、状态变化等。
## 
## 行为类型：
## - 触发行为：响应特定事件的一次性行为
## - 持续行为：具有持续时间的行为
## - 条件行为：基于条件判断的行为
## - 组合行为：由多个子行为组成的复合行为
## 
## 功能特性：
## - 与行为组件集成
## - 可变参数支持
## - 灵活的执行时机
## - 可扩展的行为系统
@abstract class_name Action
extends Node2D

## 绑定的行为组件
## 指向拥有此行为的行为组件实例，用于访问实体信息
var c_action: IComponent

## 执行行为效果
## 行为的核心逻辑实现，支持可变参数传递
## @param _args: 可变参数列表，用于传递行为执行所需的数据
@abstract func _effect(..._args)

## 执行行为（便捷方法）
## 提供统一的行为执行接口，内部调用_effect方法
## @param args: 传递给_effect的参数
func _execute(...args):
	_effect(args)
