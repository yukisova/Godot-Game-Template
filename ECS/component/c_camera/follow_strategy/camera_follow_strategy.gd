## @editing: Sora [br]
## @describe: 相机跟随策略基类 - 定义相机跟随行为的抽象接口
## 
## 该抽象类为所有相机跟随策略提供统一的接口。相机跟随策略采用策略模式设计，
## 允许在运行时动态切换不同的相机行为，提供丰富的相机体验。
## 
## 策略类型：
## - 直接跟随：相机直接跟随目标位置
## - 平滑跟随：带有缓动效果的相机跟随
## - 鼠标辅助：结合鼠标位置的相机偏移
## - 预测跟随：根据移动方向预测相机位置
## - 区域限制：在特定区域内的相机跟随
## 
## 功能特性：
## - 策略模式设计
## - 与相机组件无缝集成
## - 可配置的跟随参数
## - 平滑过渡支持
@abstract class_name CameraFollowStrategy
extends Node

## 绑定的相机组件
## 策略将作用于此相机组件，由相机组件在初始化时设置
var c_camera: C_Camera

## 执行跟随策略
## 每帧调用的核心相机跟随逻辑实现
## @param _delta: 帧时间间隔
@abstract func _strategy(_delta: float) -> void
